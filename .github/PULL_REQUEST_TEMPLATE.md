## What

<!-- What does this PR change, and why? Link the related issue if one exists. -->

## Checklist

- [ ] `npm run build && npm run lint && npm test` pass locally
- [ ] Tests added/updated for changed behavior
- [ ] `version` bumped in `package.json` if this should publish a release on merge
- [ ] Docs (`README.md` / examples / `CHANGELOG.md`) updated if behavior or config changed
